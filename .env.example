# ---------------------------------------------------------------------------
# Market Scanner configuration. Copy to `.env` and edit. All values optional.
# ---------------------------------------------------------------------------

# Which market-data sources to use, in order. The first usable answer wins.
#   auto    - (default) tradier (if a token is set), then yahoo, cboe, stooq
#   tradier - real-time quotes + option greeks (needs TRADIER_TOKEN)
#   yahoo   - free, no key; quotes with extended hours, chains, history
#   cboe    - free, no key; option chains + underlying, straight from the
#             exchange. No cookie or crumb handshake, so it keeps working
#             when Yahoo starts blocking.
#   stooq   - free, no key; daily bars as plain CSV. History only.
#
# A comma-separated list sets the order explicitly, e.g. "cboe,stooq".
#
# There is no simulated or offline option. Every price the app shows is a
# price that actually traded; when a provider is unreachable it serves the
# most recent real data it already fetched, labelled with its age.
# Both providers report pre- and post-market prices.
MARKET_DATA_PROVIDER=auto

# Tradier: free developer token at https://developer.tradier.com/
# Sandbox tokens work but return delayed data; a funded brokerage account
# token returns true real-time quotes and greeks.
TRADIER_TOKEN=
TRADIER_BASE_URL=https://api.tradier.com/v1

# Server
HOST=127.0.0.1
PORT=8000

# SQLite location
DB_PATH=data/market_scanner.db

# Comma-separated extra tickers to always include in the scan universe
EXTRA_UNIVERSE=
