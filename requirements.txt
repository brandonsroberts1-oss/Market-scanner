fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
numpy>=1.26
python-dateutil>=2.9

# Windows does not ship the IANA time zone database that `zoneinfo` reads, so
# the app needs it from PyPI. Harmless everywhere else.
tzdata>=2024.1; sys_platform == "win32"

# dev
pytest>=8.0
